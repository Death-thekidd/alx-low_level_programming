#include "main.h"

/**
 * strtow - splits a string into words
 * @str: string to split
 *
 * Return: two-dimensional array
*/
char **strtow(char *str)
{
	int i, height;
	char **split_str;

	if (str == NULL || *str == '\0')
		return (NULL);
}
